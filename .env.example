# Copy to ".env" for local dev, or set these in Vercel → Settings → Environment Variables.

# Your Google AdSense publisher ID. Leave blank to disable ads entirely.
# Find it in AdSense → Account → Settings (looks like ca-pub-1234567890123456).
VITE_ADSENSE_CLIENT=
